<%= form_with(model: cloth.relationship_categories.build, local: true) do |f| %>
  <%= hidden_field_tag :category_id, category.id %>
  <%= hidden_field_tag :cloth_id, cloth.id %>
  <%= f.submit '追加', class: 'btn btn-outline-primary btn-small' %>
<% end %>